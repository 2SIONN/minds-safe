# ── 패키지 & 잠금파일
node_modules/
npm-debug.log*
yarn-error.log*
pnpm-lock.yaml
bun.lockb

# ── Next.js / 빌드 산출물
.next/
.next/static/
.next/cache/
out/
dist/
build/
*.tsbuildinfo

# ── 번들러/도구 캐시
.turbo/
.vite/
coverage/

# ── 환경 변수
.env*
!.env.example   # 템플릿은 추적

# ── Prisma (로컬 DB/저널)
prisma/*.db
prisma/*.db-journal

# ── 업로드/임시 파일(프로젝트에 따라 경로 조정)
uploads/
tmp/
*.log

# ── OS / IDE
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
.history/

# ── Docker (프로젝트에 따라)
docker-data/

# ── Vercel
.vercel/

# ── Storybook (사용 시)
storybook-static/

# ── 테스트 스냅샷 (사용 시)
*.snap

# ── 이미지/대용량은 LFS 권장(선택)
# .gitattributes 에서 관리: git lfs track "*.png" "*.jpg" "*.mp4" "*.zip"
